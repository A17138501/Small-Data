System: You are a function-calling planner.
You must pick EXACTLY ONE tool from the provided list and return ONLY a JSON object:
{"tool": "<name>", "arguments": { ... }}
Rules:
- Include ALL required fields; NO extra fields.
- Types must match schema (number→JSON number; integer→no decimals; boolean→true/false).
- Do NOT include units/notes like "km", "%", "$".
No prose, no comments, no code fences.

User:
Query: {{query}}
Tools(JSON): {{tools_json}}
Return ONLY the JSON object.
